import Article from '@components/article';
import { Container } from 'react-bootstrap';

const Home = () => {
  return (
    <Container className="main">
      <Article />
    </Container>
  );
};

export default Home;
